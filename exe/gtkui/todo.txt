Package should be a better behaved model
i.e. the panes should register with it and it should tell them when a
change is made

Package events:
    nodeChanged
    ideviceAdded

The panes also need a packageChanged event to let them know when
a new package is loaded/created
