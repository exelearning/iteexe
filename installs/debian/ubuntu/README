Building from source on Ubuntu

1. Make sure you have build tools installed:
 $ apt-get install build-essential

2. Install a couple python modules
 $ apt-get install python-feedparser python-beautifulsoup

3. Clone the git repo
 $ git clone git clone https://forja.cenatic.es/anonscm/git/iteexe/iteexe.git
 
4. cd to directory and run the make process
 $ cd <checkoutdir>/iteexe/installs/debian/ubuntu
 $ python make.py
 

